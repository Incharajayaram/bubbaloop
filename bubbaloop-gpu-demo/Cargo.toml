[package]
name = "bubbaloop-gpu-demo"
version = "0.1.0"
edition = "2021"
description = "Webcam to kornia-rs imgproc benchmark"

[dependencies]
kornia = { version = "0.1.10-rc.3", features = ["v4l"] }

[[bin]]
name = "bubbaloop-gpu-demo"
path = "src/main.rs"

[workspace]
